name: dars10
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  dio: ^5.0.0
  firebase_core: ^2.6.1
  get_it: ^7.2.0
  path_provider: ^2.0.12
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

#  rm /ios/Pods; rm /ios/Podfile.lock; cd ios; Run pod install --repo-update; sudo arch -x86_64 gem install ffi; arch -x86_64 pod install --repo-update; flutter clean; flutter pub get